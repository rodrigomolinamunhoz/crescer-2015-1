package Listator;

import java.util.*;

public class Filme {
	private Ator nome;
	List<Ator> listaAtores = new ArrayList<Ator>();  
	
	
	public void insereAtor(Ator ator){
		this.listaAtores.add(ator);
	}
	public static void main(String[] args) {
	}
	
	
	public void listaAtores(Ator nome){
		for(Ator atual : listaAtores) {    
			if (atual.) {
				
			}
		}
		
		string myString = "bla";
		string element = "";

		for (int i = 0; i < myList.Count; i++)
		{
		    if (myList[i].Contains(myString))
		        element = myList[i];
		}

	}
	
	public Ator getNome() {  
		return this.nome;  
	} 
}
